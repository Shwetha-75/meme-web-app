import styled from "styled-components";
import "./previous.css"

const Button = styled.button`
       
`

export default function PreviewButton() {
  return (
    <Button className="previous--btn--tag p-3 w-[30%]">
        Preview
    </Button>
  )
}
